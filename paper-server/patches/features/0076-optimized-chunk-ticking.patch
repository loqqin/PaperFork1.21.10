From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: aaa <121246396+loqqin@users.noreply.github.com>
Date: Tue, 18 Aug 2026 17:03:33 +0300
Subject: [PATCH] optimized chunk ticking


diff --git a/net/minecraft/server/level/ServerChunkCache.java b/net/minecraft/server/level/ServerChunkCache.java
index 075e294333115738b25b64a390df323276cf39f9..6c66a642f5d68bf25d50385b99aa482d5931f8c7 100644
--- a/net/minecraft/server/level/ServerChunkCache.java
+++ b/net/minecraft/server/level/ServerChunkCache.java
@@ -579,17 +579,21 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
         List<LevelChunk> spawningChunks = this.spawningChunks;
 
         try {
-            profiler.popPush("filteringSpawningChunks");
-            this.chunkMap.collectSpawningChunks(spawningChunks);
-            profiler.popPush("shuffleSpawningChunks");
-            // Paper start - chunk tick iteration optimisation
-            this.shuffleRandom.setSeed(this.level.getRandom().nextLong());
-            if (!this.level.paperConfig().entities.spawning.perPlayerMobSpawns) Util.shuffle(spawningChunks, this.level.getRandom()); // Paper - Optional per player mob spawns; do not need this when per-player is enabled
-            // Paper end - chunk tick iteration optimisation
-            profiler.popPush("tickSpawningChunks");
-
-            for (LevelChunk chunk : spawningChunks) {
-                this.tickSpawningChunk(chunk, timeDiff, spawningCategories, spawnCookie);
+            if (!spawningCategories.isEmpty()) {
+                profiler.popPush("filteringSpawningChunks");
+                this.chunkMap.collectSpawningChunks(spawningChunks);
+                profiler.popPush("shuffleSpawningChunks");
+                // Paper start - chunk tick iteration optimisation
+                this.shuffleRandom.setSeed(this.level.getRandom().nextLong());
+                if (!this.level.paperConfig().entities.spawning.perPlayerMobSpawns) Util.shuffle(spawningChunks, this.level.getRandom()); // Paper - Optional per player mob spawns; do not need this when per-player is enabled
+                // Paper end - chunk tick iteration optimisation
+                profiler.popPush("tickSpawningChunks");
+
+                for (LevelChunk chunk : spawningChunks) {
+                    this.tickSpawningChunk(chunk, timeDiff, spawningCategories, spawnCookie);
+                }
+            } else {
+                tickInhabitedTimeOnly(timeDiff);
             }
         } finally {
             spawningChunks.clear();
@@ -605,6 +609,30 @@ public class ServerChunkCache extends ChunkSource implements ca.spottedleaf.moon
         profiler.pop();
     }
 
+    private void tickInhabitedTimeOnly(long timeInhabited) {
+        if (this.level.getGameTime() % 20L != 0L) {
+            return;
+        }
+
+        long delta = 20L * timeInhabited;
+        int radius = 8;
+
+        it.unimi.dsi.fastutil.objects.ReferenceOpenHashSet<LevelChunk> visited = new it.unimi.dsi.fastutil.objects.ReferenceOpenHashSet<>();
+
+        for (ServerPlayer player : this.level.players()) {
+            ChunkPos pos = player.chunkPosition();
+
+            for (int dx = -radius; dx <= radius; dx++) {
+                for (int dz = -radius; dz <= radius; dz++) {
+                    LevelChunk chunk = this.getChunkAtIfLoadedImmediately(pos.x + dx, pos.z + dz);
+                    if (chunk != null && visited.add(chunk)) {
+                        chunk.incrementInhabitedTime(delta);
+                    }
+                }
+            }
+        }
+    }
+
     private void tickSpawningChunk(
         final LevelChunk chunk, final long timeDiff, final List<MobCategory> spawningCategories, final NaturalSpawner.SpawnState spawnCookie
     ) {
